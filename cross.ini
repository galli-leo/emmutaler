[constants]
bin_dir = toolchain / 'bin'

[binaries]
c = bin_dir / arch + '-gcc'
cpp = bin_dir / arch + '-g++'
strip = bin_dir / arch + '-strip'
ar = bin_dir / arch + '-ar'

[built-in options]
includedir = include